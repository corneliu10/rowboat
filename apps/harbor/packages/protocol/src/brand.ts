// Wire-level rebrand values shared by Harbor and apps/x (see
// apps/x/packages/shared/src/brand.ts — the two copies must match; the
// apps/x test brand.test.ts asserts it so they cannot drift).
export const MENTION_HANDLE = "spinball";
export const DEEP_LINK_SCHEME = "spinrun";
