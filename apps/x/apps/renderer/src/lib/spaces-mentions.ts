// The @spinball trigger reads the mention TOKEN (protocol mentions.ts) through
// @x/shared's face of it; this module keeps the renderer's import path.
// Kept name containsRowboatAddress: ripples through protocol types; value is now spinball.
export { containsRowboatAddress } from '@x/shared/dist/spaces.js'
