# spike/e-harbor
Base: `ac9db19765967c51b6409aee8c516638a116abf8` (origin/spike/base) · Head: this report commit (HEAD; `git log --oneline -4` to verify) · `git diff --stat origin/spike/base...HEAD`: 5 files changed, 345 insertions(+)
## Commits
- `eae03fc3` e-harbor goal 1: dev-mode Space via desktop sandbox + onboarding screenshot (goal 1)
- `5ce97b10` e-harbor goal 2: harbor deployment-mode docker-compose + env example (goal 2)
- `c64b6ac2` e-harbor goal 3: docs/fork/05-harbor.md (modes, commands, OIDC assessment) (goal 3)
- HEAD (this commit) e-harbor report: docs/fork/reports/e-harbor.md (report)
## Tests
- harbor (`cd apps/harbor && pnpm test` = `pnpm -r test`, server `vitest run`): default parallelism RED on PGlite hook timeouts — run A: files 16 failed / 9 passed (25), tests 5 failed / 244 passed / 133 skipped (382); run B (sandbox stopped): files 14 failed / 11 passed, tests 3 failed / 254 passed / 125 skipped. Every failure is a `(postgres store)` / `pgliteDb()` `beforeAll` timeout (e.g. `test/pg-store.test.ts:24` `Hook timed out in 10000ms`); all memory-store suites pass; zero `apps/harbor` files changed in this lane. Isolated `test/members.test.ts`: 8/8 pass in 12 s (postgres variant included). Bounded rerun `vitest run --maxWorkers=2`: **25 files passed (25), 381 passed + 1 skipped (382), exit 0, 45 s** — the 1 skip is the S3 conformance suite (`HARBOR_TEST_S3_BUCKET` unset, blobs.test.ts:36,129).
- typecheck (`pnpm typecheck`: protocol + server `tsc --noEmit`): exit 0.
- apps/x suites: not run in this lane (acceptance names harbor's only; `apps/x` source untouched).
## Acceptance
| # | item (verbatim from the prompt) | pass / fail / blocked | evidence (a test name, a file:line, a number) |
| 1 | dev-mode Space created and a message posted from the desktop, screenshot | pass | `e-harbor lane` space `01M2R78RGRW9HJEAFEZ72TMAYH` created via the sandbox desktop's core path (`addDevOrg` orgs.ts:323 → `createSpace`, `postMessage` ×2 offsets 2,3, `mentionsRowboat:true` on the `[@rowboat](#rowboat)` post); org record in `.sandbox/rowboat-e-harbor/config/spaces_orgs.json`; desktop trio `server=50218 apps=50219 vite=50220` → 401/421/200; `docs/fork/img/05-spaces.png` (1092006 bytes) shows the sandbox desktop — onboarding modal, not the Space (see Not verified) |
| 2 | compose file starts postgres + harbor (`docker compose ps` pasted) and the OIDC-less behaviour is recorded verbatim | pass | `ps -a`: `harbor-e-harbor-harbor-1 ... Exited (1)`, `harbor-e-harbor-postgres-1 ... Up (healthy)`; image `harbor-e-harbor-harbor:latest` 351 MB; `logs harbor` verbatim `HARBOR_MODE=deployment requires AUTH_ISSUER` (main.ts:99-103, exit 1, :4273 refused curl exit 7); dummy-issuer probe (uncommitted `-e`): `/healthz` `{"ok":true}`, unknown domain 404 `no org on this domain`, apex no-token 401 `missing bearer token` + `WWW-Authenticate ...resource_metadata=...`, garbage bearer `internal ... authorization server discovery failed for https://dummy.invalid/auth/v1` |
| 3 | the OIDC assessment written with file:line citations | pass | `docs/fork/05-harbor.md` §3: discovery order auth-oidc.ts:110-114, issuer/jwks checks :125-129, algs :52-56, no-auto-create :78-84, aud-off :18-22; publishable key consent.ts:20-27,103-133, GoTrue implication :9-11; Supabase flagship auth-oidc.ts:16, consent.ts:21, CONTRACT.md:30-37 |
| 4 | harbor's own tests green with counts (`cd apps/harbor && pnpm test` or what package.json names) | pass | `--maxWorkers=2`: 25 files passed, 381 passed + 1 skipped (382), exit 0; typecheck exit 0; default-parallelism red recorded above with file:line, never omitted |
| 5 | report at docs/fork/reports/e-harbor.md | pass | this file |
| 6 | pushed | pass | `git status -sb` → `## spike/e-harbor...origin/spike/e-harbor` after push; upstream push URL `DISABLED`; no PR opened |
## Decisions
- Corepack shim resolved `pnpm@12.4.2` (no `packageManager` pin in `apps/x`, shim default) and its `bin/pnpm.cjs` layout is unreadable by corepack 0.35.0 (`MODULE_NOT_FOUND`); `apps/x` install + `npm run deps` ran via the real binary `node ~/.cache/node/corepack/v1/pnpm/10.34.5/dist/pnpm.cjs` (exit 0, ~338 s install, deps chain exit 0). Harbor commands ran via pnpm 10.20.0 the same way (its `packageManager` pin).
- Postgres trust auth in compose: `POSTGRES_HOST_AUTH_METHOD=trust` so no password exists anywhere near a tracked file (guard rail). Labeled local-spike-only in both files.
- "Add the server by address" was exercised through the desktop sandbox instance's own code path (same `orgs.addDevOrg` / `getClient().createSpace/postMessage` the `spaces:*` IPC handlers call, same `ROWBOAT_WORKDIR`) rather than clicked UI: the sandbox boots (Electron pid observed, ports 401/421/200) but its first-run UI is the onboarding gate.
- Dummy-issuer probe used an uncommitted `docker compose run -e AUTH_ISSUER=...` one-off (container `harbor-e-dummy`, removed after); tracked `.env.example` keeps `AUTH_ISSUER=` empty.
- Full-suite red is load flakiness (PGlite boot × parallel workers under lane contention), proven by the isolated 8/8 and the `--maxWorkers=2` all-green rerun; no source fix attempted (nothing in `apps/harbor` changed, nothing to fix).
- Sandbox stack stopped after Goal 1 (ports verified 000); Harbor dev :4272 left running for the record, stopped before push (see commands below). Compose stack left `postgres Up / harbor Exited(1)` for the record, then `down` (volumes kept, named `harbor-e-*`).
## Not verified
- Agent reply to the `@rowboat` mention: not tested, no model key (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`OPENROUTER_API_KEY`/`OLLAMA_HOST`/`ROWBOAT_API_KEY` all absent, sandbox `models.json` 31 bytes with no `apiKey`); stream holds exactly the 2 posted messages.
- In-app Space screenshot: blocked — fresh-workdir onboarding (`showOnboarding:true`) gates the Spaces UI and there is no provider to complete it with; `05-spaces.png` is the genuine desktop onboarding screen. A first `screencapture` caught the host screen instead and was deleted, never committed.
- Live check against spinrun's Supabase Auth project: paper assessment only — no credentials exist in this environment and none were created (per prompt).
- `apps/x` vitest suites and the Electron package step: not run in this lane (baseline covers them on the same base tip).
## Shared files
None. No file another lane owns was touched: `git diff --stat origin/spike/base...HEAD` lists only `deploy/harbor/.env.example`, `deploy/harbor/docker-compose.yml`, `docs/fork/05-harbor.md`, `docs/fork/img/05-spaces.png`, `docs/fork/reports/e-harbor.md`. LICENSE and all upstream headers intact; `POSTHOG_KEY` / `VITE_PUBLIC_POSTHOG_KEY` never set (verified absent before dev/package runs; server logged `[Analytics] POSTHOG_KEY not set; analytics disabled`).
