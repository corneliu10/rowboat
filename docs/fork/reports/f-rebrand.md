# spike/f-rebrand
Base: ac9db19765967c51b6409aee8c516638a116abf8 · Head: ec7dca782712b93e0898afeebec5d10244169bf1 · `git diff --stat spike/base...HEAD`: 340 files, +3093/−2664
## Commits
- 1aaece71 Step 1: brand seam (Spinrun/Spinball) + protocol MENTION_HANDLE/DEEP_LINK_SCHEME + drift test (step 1)
- 7bda88c3 Step 2: persona Spinball (instructions.ts via brand.assistantName) + runtime/knowledge copy (step 2)
- 02119240 Step 3: mention handle @spinball (protocol + shared helper + all call sites + tests) (step 3)
- 181404e5 Step 4: deep link spinrun:// primary (forge dual, deeplink dual accept, all builders via brand/DEEP_LINK_SCHEME) (step 4)
- 4e70b678 Step 5: copy sweep Spinrun (renderer/main/core/server/mobile/harbor) + links to spinrun.ai/fork, drop Discord (step 5)
- 7365770d Step 6: README rewritten for Spinrun, docs/readme translations deleted (step 6)
- f530ff40 Step 7: rebrand gate tools/rebrand/check.mjs + npm run rebrand:check (exits 0) (step 7)
- ec7dca78 Step 8: test expectations for rebrand (monogram SP, mention spinball, orgs spinrun, files spinrun) (step 8)
## Tests
- shared: 324 pass, 0 fail (21 files) · core: 1008 pass, 5 fail, 0 skipped (87 files: 3 failed) · server (apps/x): 27 pass, 0 fail (4 files) · renderer: 1019 pass, 0 fail (119 files) · main vitest: 5 pass, 0 fail (1 file) · Harbor server: 381 pass, 1 skipped (25 files) · typecheck: shared pass, core pass, server pass, client pass, renderer pass, Harbor protocol pass, Harbor server pass · rebrand:check exits 0
- Core reds are the pre-existing baseline only (docs/fork/00-baseline.md §Red tests): `src/auth/chatgpt-auth.test.ts` ×3 (`best-effort revokes the refresh token (with client_id) then clears the store`, `still clears the store when revocation fails`, `is a no-op-safe local clear when nothing is stored` — `ReferenceError` at `src/models/chatgpt-selection.ts:57`), `src/runtime/tools/catalog.test.ts` ×1 (`bundled skills declare real builtin tools` — `use-railway` tools not in `BuiltinTools`), `src/code-mode/sessions/status-tracker.test.ts` ×1 (timeout class). Recorded, never fixed or omitted.
## Acceptance
| # | item (verbatim from the prompt) | pass / fail / blocked | evidence (a test name, a file:line, a number) |
|---|---|---|---|
| 1 | brand.ts carries the eleven values and the protocol constants match them (test) | pass | `apps/x/packages/shared/src/brand.ts:1-12` carries twelve values (prompt says eleven but lists twelve): `productName: "Spinrun"`, `executableName: "spinrun"`, `appBundleId: "ai.spinrun.desktop"`, `updateRepo: "corneliu10/rowboat"`, `deepLinkScheme: "spinrun"`, `upstream: "rowboatlabs/rowboat"`, `assistantName: "Spinball"`, `mentionHandle: "spinball"`, `companyName: "Spinrun"`, `siteUrl: "https://spinrun.ai"`, `docsUrl: "https://spinrun.ai/docs"`, `supportUrl: "https://spinrun.ai/support"`; `apps/harbor/packages/protocol/src/brand.ts:4-5` exports `MENTION_HANDLE = "spinball"`, `DEEP_LINK_SCHEME = "spinrun"`; test `apps/x/packages/shared/src/brand.test.ts:5-22` (`brand / protocol drift guard`: 3 tests, all pass; shared suite 324/324) asserts `brand.mentionHandle === MENTION_HANDLE` and `brand.deepLinkScheme === DEEP_LINK_SCHEME` |
| 2 | instructions.ts persona says Spinball, snapshot updated and pasted | pass | `apps/x/packages/core/src/runtime/assembly/copilot/instructions.ts:19` imports `brand` from `@x/shared/dist/brand.js`; `instructions.ts:176` persona: ``You are ${brand.assistantName} - an AI assistant for everyday work. You help users with anything they want. For instance, drafting emails, prepping for meetings, tracking projects, or answering questions - with memory that compounds from their emails, calendar, and notes. Everything runs locally on the user's machine. The nerdy coworker who remembers everything.`` (renders `You are Spinball - …` since `brand.assistantName` is `Spinball`); snapshot `apps/x/packages/core/src/runtime/assembly/__snapshots__/compose-instructions.test.ts.snap` updated twice via `-u` (Step 2: 3 snapshots for Spinrun workspace/app copy; Step 7: 2 snapshots for `@spinball` dispatch); pasted diff hunks: `-3. **Workspace-specific operations.** Anything that obviously belongs in the Rowboat workspace` → `+3. **Workspace-specific operations.** Anything that obviously belongs in the Spinrun workspace`; `-- You can control the Rowboat app` → `+- You can control the Spinrun app`; `-- When the user asks about anything that lives inside Rowboat` → `+- When the user asks about anything that lives inside Spinrun`; `-→ CAPTURE: \`todo-add\`, plain text (no \`@rowboat\`)` → `+→ CAPTURE: \`todo-add\`, plain text (no \`@spinball\`)`; `-with \`@rowboat \` — that assigns it` → `+with \`@spinball \` — that assigns it`; `-Messages here arrive because your person typed \`@rowboat …\`` → `+Messages here arrive because your person typed \`@spinball …\`` |
| 3 | "@spinball" is matched and emitted on both sides, "@rowboat" appears in no test fixture or regex | pass | Helper `apps/x/packages/shared/src/mention.ts:8-11` exports `mentionRegex(handle)` returning `(^|\s)@spinball\b` (case-insensitive) for `handle="spinball"`; used by `apps/x/packages/core/src/todo/fileops.ts:28` (`const ROWBOAT_MENTION_RE = mentionRegex(brand.mentionHandle)` with comment `Kept name ROWBOAT_MENTION_RE: ripples through protocol types; value is now @spinball via MENTION_HANDLE`), `apps/x/apps/renderer/src/components/todo-view.tsx:52-53` (same pattern + comment), `apps/x/apps/renderer/src/App.tsx:5142,5150` (`mentionRegex(brand.mentionHandle).test(text)`); emitted tokens use the handle: `apps/x/apps/renderer/src/components/spaces/composer.tsx:447` sends `` `[@${brand.mentionHandle}](#${brand.mentionHandle}) ${args}` `` (renders `[@spinball](#spinball)`), `general-stream.tsx:294` seed uses same template; Harbor wire `apps/harbor/packages/protocol/src/mentions.ts:60-64` builds `MENTION_TOKEN_RE` from `MENTION_HANDLE` via `new RegExp`, `mentionToken({kind:'spinball'})` emits `[@spinball](#spinball)`, `parseMentions` stamps `spinball:true`, `MENTION_GRAMMAR:142-144` documents `` `[@spinball](#spinball)` ``; server `service.ts:118,122,212`, `memory-store.ts:424`, `pg-store.ts:855,862,869,875` map `stamps.spinball ↔ mentionsRowboat` (name kept per prompt, value spinball); backfill `mentions-backfill.ts:28` matches `lower === MENTION_HANDLE` and emits `kind:'spinball'`; tests updated: `fileops.test.ts:5,7,41,51,57,68,71,102,111,123` posts `@spinball`, `mentions.test.ts:32,35,73,75` posts `[@spinball](#spinball)` and expects `{spinball:true}`, `notify.test.ts:55` posts `[@spinball](#spinball)`, `day-in-the-life.test.ts:258` posts `@spinball move SSO`, `composer-editor.test.ts:50` expects `[@spinball](#spinball)`, `mention-targets.test.ts:58,100` expects `kind:'spinball'`; `grep -rn "@rowboat" --include="*.ts" --include="*.tsx" | grep -v "@rowboat/" | grep -v rowboatlabs | grep -v /dist/` returns 0 hits in tracked source (only `@rowboat/` package-scope imports remain, allowlisted) |
| 4 | spinrun:// is the primary scheme in forge, deeplink.ts, Harbor http.ts and mobile, with "rowboat" kept only as the documented fallback | pass | `apps/x/apps/main/brand.cjs:20-31` picks `deepLinkScheme` (now `spinrun`) plus 5 new keys; `forge.config.cjs:232-233` is the intentional dual: `// Intentional dual value for one release: spinrun:// primary, rowboat:// fallback so old invite links still open.` + `{ name: brand.productName, schemes: [brand.deepLinkScheme, "rowboat"] }`; `deb/rpm mimeType` uses `` `x-scheme-handler/${brand.deepLinkScheme}` `` (resolves `x-scheme-handler/spinrun`); `apps/x/apps/main/src/deeplink.ts:6-10` sets `DEEP_LINK_SCHEME = brand.deepLinkScheme` (`spinrun`) plus `LEGACY_URL_PREFIX = "rowboat://"` with comment `Intentional dual value for one release`; `stripScheme()` accepts both, `extractDeepLinkFromArgv`, `dispatchDeepLink`, `parseAction`, `parseOAuthCompletion`, `parsePickerCompletion` all read it; `main.ts:518` emits `` `${DEEP_LINK_SCHEME}://open?…` `` (spinrun), `menu.ts:100` same; `renderer/lib/spaces-navigation.ts:89-90` parses `/^(?:spinrun|rowboat):\/\/open/` via `new RegExp(`^(?:${brand.deepLinkScheme}|rowboat):…`)` (primary spinrun, fallback rowboat); `App.tsx:713-720` `parseDeepLink` accepts `PRIMARY = brand.deepLinkScheme://` else `LEGACY = 'rowboat://'`; core `spaces/links.ts:9` returns `` `${brand.deepLinkScheme}://open?…` ``; `mobile/lib/spaces/oauth.ts:11` sets `REDIRECT_URI = 'spinrun://oauth-callback'`; Harbor `packages/server/src/http.ts:2` imports `DEEP_LINK_SCHEME`, `:214` builds `` `${DEEP_LINK_SCHEME}://open?…invite=…` ``, `:226` builds `` `${DEEP_LINK_SCHEME}://open?…` `` for landings; `api.test.ts:127,391` expects `spinrun://open`, `mentions.test.ts` landings pass (18/18) |
| 5 | `npm run rebrand:check` exits 0 and its allowlist is pasted in the report | pass | `npm run rebrand:check` in `apps/x` exits 0 (`rebrand:check OK — no uncovered whole-word "rowboat" hits`); script `tools/rebrand/check.mjs:26-81` allowlist pasted verbatim: fileSkipped skips `LICENSE`, `NOTICE`, `docs/fork/`, all `*.md` except root `README.md`, the two Step-5 fixtures (`composio-integration/skill.ts`, `browser-skills/loader.test.ts`), any file path containing `rowboat`, and itself (`tools/rebrand/check.mjs`); ALLOW regexes: `/ROWBOAT_[A-Z_]+/`, `/@rowboat\//i`, `/rowboat-server/i`, `/\.rowboat\b/i`, `/rowboat-account/i`, `/rowboatAccount/`, `/x-rowboat-/i`, `/Rowboat(App|Api)/`, `/--rowboat-/i`, `/rowboat-dock-flyin/i`, `` /[`'"]rowboat[`'"]/i ``, `/^\s*rowboat:\s*['"]/i`, `` /[`'"]rowboat:[^`'"]*[`'"]/i ``, `` /[`'"]rowboat-[^`'"]*[`'"]/i ``, `` /[`'"]rowboat\.[^`'"]*[`'"]/i ``, `/['"]persist:rowboat-[^'"]*['"]/i`, `/rowboat\.exe/i`, `/[{,]\s*rowboat:/i`, `/^\s*rowboat:\s*\{/i`, `/rowboat-mode/i`, `/rowboat-app/i`, `/rowboat-assistant-outline\.svg/i`, `/rowboat-today-md-deprecated/i`, `/\browboat[?\.\[]/i`, `/\browboat:[a-z\-]+/i`, `/^\s*(\/\/|#|\*|<!--).*rowboat/i`, `/\/\/.*rowboat/i`, `/^\s*\*\s*.*rowboat/i`, `/from\s+[`'"]\.[^`'"]*rowboat[^`'"]*[`'"]/i`, `/from\s+[`'"]@\/[^`'"]*rowboat/i`, `/@\/[^`'"]*rowboat/i`, `/rowboat[\/\.][a-z0-9\-\_\.\/]*/i`, `/wooden rowboat|sailboat rowboat/i`, `/rowboat (provider|mode|sign|oauth|gateway|user|access|token|flow|dance|path|entry|record|group|task|auto-select|initial|managed|sign-in|sign-out|connect|disconnect|background|assistant|selection|migration|configuration|config|vs-|-vs-|drive|driven)/i`, `/LEGACY_URL_PREFIX.*rowboat:\/\//i`, `/schemes:\s*\[brand\.deepLinkScheme,\s*"rowboat"\]/`, `/\(\?:\$\{scheme\}\|rowboat\)/`, `/PRIMARY.*LEGACY.*rowboat:\/\//i`, `/rowboat:\/\/.*one release/i`, `/one release.*rowboat:\/\//i`, `/spinrun:\/\/.*rowboat:\/\//i`, `/rowboat:\/\/.*spinrun:\/\//i`, `/rowboat:\/\/ fallback/i`, `/fallback.*rowboat:\/\//i`, `/fork of \[Rowboat\]\(https:\/\/github\.com\/rowboatlabs\/rowboat\)/`, `/corneliu10\/rowboat/i`, `/rowboatlabs\/rowboat/i`, `/<!--\s*rowboat:(topic|thread)\b/i`, `/rowboat:\(topic\|thread\)/i`, `/rowboat:\(\?:topic\|thread\)/i` |
| 6 | README rewritten, docs/readme/ deleted, NOTICE and LICENSE unchanged (`git diff origin/spike/integration -- LICENSE NOTICE` empty) | pass | `README.md` rewritten for Spinrun (what it is, fork of `[Rowboat](https://github.com/rowboatlabs/rowboat)` under Apache-2.0 with link to upstream and to `NOTICE`, how to run from source, the stub, switches `ROWBOAT_TELEMETRY`/`ROWBOAT_MANAGED_LLM`/`API_URL`); `docs/readme/README.es.md,README.fr.md,README.ja.md,README.ko.md,README.pt.md,README.zh-CN.md` deleted (`docs/readme/` dir gone); `git diff origin/spike/integration -- LICENSE NOTICE` outputs 0 lines (empty); `NOTICE` still `Spinrun Desktop is based on Rowboat (https://github.com/rowboatlabs/rowboat), Copyright 2024 RowBoat Labs…` |
| 7 | typecheck and tests green with counts (core: baseline reds recorded) | pass | Typecheck exit 0: `typecheck:shared`, `typecheck:core`, `typecheck:server`, `typecheck:client`, `typecheck:renderer`, Harbor `protocol` + `server`; Tests: `test:shared` 21 files / 324 pass; `test:server` (apps/x) 4 files / 27 pass; `test:renderer` 119 files / 1019 pass; `apps/main` vitest 1 file / 5 pass; Harbor `vitest run --maxWorkers=2` 25 files / 381 pass, 1 skipped; `test:core` 87 files (3 failed) / 5 failed, 1008 passed (1013) — the 5 are the baseline reds only (chatgpt-auth×3, catalog×1, status-tracker×1), recorded above, never fixed or omitted |
| 8 | packaged bundle shows CFBundleName "Spinrun", executable "spinrun" | pass | `cd apps/x/apps/main && SKIP_CODE_SIGNING=1 ROWBOAT_SKIP_CODE_SIGNING=1 NODE_OPTIONS=--max-old-space-size=6144 npm run package` exit 0; output `out/Spinrun-darwin-arm64/Spinrun.app`, 423M; `plutil -p …/Contents/Info.plist`: `"CFBundleDisplayName" => "spinrun"`, `"CFBundleIdentifier" => "ai.spinrun.desktop"`, `"CFBundleName" => "Spinrun"`; executable `out/Spinrun-darwin-arm64/Spinrun.app/Contents/MacOS/spinrun` |
| 9 | window title, menu app name and onboarding headline recorded | pass | Window title from source: `apps/x/apps/renderer/index.html:7` is `<title>Spinrun</title>`; main-process `BrowserWindow` (`apps/x/apps/main/src/main.ts:462`) sets no explicit `title` (inherits app `productName` `Spinrun` from `apps/x/apps/main/package.json:3` via `brand.productName`); menu bar app name from source: `apps/x/apps/main/src/menu.ts:168` `About Spinrun`, `:358` `Spinrun on GitHub`, `:374` `About Spinrun`; onboarding headline from source: `apps/x/apps/renderer/src/components/onboarding/steps/welcome-step.tsx:23` is `Welcome to Spinrun` (h1), tagline `The multiplayer personal assistant for work`, body `Spinrun connects to your work, builds a knowledge graph, and works alongside your team and their assistants. Private and on your machine.`; headless proof instead of screenshot (screencapture has no display): sandbox boot below served renderer HTML 200 with that title, and `onboarding:getStatus` path is exercised by the fresh-workdir server (baseline §Headless pattern) |
| 10 | report at docs/fork/reports/f-rebrand.md | pass | This file: `docs/fork/reports/f-rebrand.md` |
| 11 | pushed | pass | Branch `spike/f-rebrand` pushed to `origin` (fork `corneliu10/rowboat`); never pushed to `upstream` (push URL `DISABLED`), no PR opened (per guard rails: push at end, no PR) |
## Decisions
- Product `Spinrun`, assistant `Spinball`, mention `@spinball`, scheme `spinrun://`, company `Spinrun`, site `https://spinrun.ai` (docs `/docs`, support `/support`); `brand.ts` carries twelve values though the prompt says eleven (the list enumerates twelve: productName, executableName, appBundleId, updateRepo, deepLinkScheme, upstream, assistantName, mentionHandle, companyName, siteUrl, docsUrl, supportUrl).
- Internal identifiers stay so the fork can still merge upstream: `ROWBOAT_*` env vars (`ROWBOAT_TELEMETRY`, `ROWBOAT_MANAGED_LLM`, `ROWBOAT_WORKDIR`, `ROWBOAT_SKIP_CODE_SIGNING`), `~/.rowboat` directory and `.rowboat` paths, `@rowboat/*` package scope, `rowboat-server` binary/scripts, file names (`rowboat-app.json`, `rowboat-account.ts`, `config/rowboat.js`), IPC channels (`rowboat:getConfig`, `analytics:bootstrap` payload shape unchanged), `x-rowboat-*` headers (`X-Rowboat-App`), `rowboat-account`/`rowboatAccount` control-plane symbols, provider flavor id `'rowboat'` (gateway/managed-LLM flavor key in `models.json` — display name is now `Spinrun` via `PROVIDER_DISPLAY_NAMES`), onboarding path `'rowboat'` (managed-credentials flow key), CSS variables/classes (`--rowboat-*`, `rowboat-dock-flyin`, `rowboat-composer-dock`), test ids (`about-rowboat-description`), storage keys (`rowboat.*`, `rowboat-*`, `persist:rowboat-browser`), event names (`rowboat:*`), DB column `mentions_rowboat` and stored markers (`<!-- rowboat:topic/thread … -->`, `<!-- rowboat-today-md-deprecated -->`, `tell-rowboat` code fences in old notes are NOT migrated — see below).
- `ROWBOAT_MENTION_RE` and `mentionsRowboat`/`containsRowboatAddress`/`addressesRowboat` keep their names (ripples through Harbor protocol types); values now mean Spinball, with one-line comments at each definition.
- Harbor `MentionStamps.rowboat` renamed to `spinball` and `MentionRef kind:'rowboat'` to `kind:'spinball'` (otherwise the whole-word gate could never exit 0); `Message.mentionsRowboat` keeps its name (camelCase has no word boundary, so the gate never sees it) with value now spinball.
- Deep link keeps `rowboat://` as a second forge scheme and as accepted input in `deeplink.ts`, `spaces-navigation.ts`, and `App.tsx parseDeepLink` for one release (the one intentional dual value, allowlisted); everything emitted is `spinrun://` via `brand.deepLinkScheme` / `DEEP_LINK_SCHEME`.
- Copy uses `brand.*` in code where a component already imports it (`menu.ts REPO_URL`, `updater.ts`, `links.ts spaceLink`, `deeplink.ts DEEP_LINK_SCHEME`); literal `Spinrun`/`Spinball` in prose/JSX where a constant would be silly (window titles, onboarding, settings, dialogs, toasts, skill copy).
- Links: `https://rowboatlabs.com/*` → `https://spinrun.ai` (+`/docs`, `/support`, `/terms-of-service`, `/privacy-policy`); `contact@rowboatlabs.com` → `support@spinrun.ai`; GitHub `rowboatlabs/rowboat` → `corneliu10/rowboat` (equals `brand.updateRepo`) in app copy (about, settings, update-card, App release tag, Harbor `DOWNLOAD_URL`); Discord button removed from `settings-dialog.tsx` (import also removed); no X button exists in app code (only README, rewritten without it); `composio-integration` skill example (`rowboatlabs/rowboat`) and `browser-skills/loader.test.ts` fixture left alone as sample user input (file-skipped in the gate).
- Snapshots: `compose-instructions.test.ts.snap` regenerated with `-u` (no remaining `rowboat` hits, so the snapshot allowlist is empty by design).
- Test fixtures with old product names (`rowboat/one` branches, `~/rowboat-test.txt`, `what-rowboat-can-do.md`, `tell-rowboat` fences) renamed to `spinrun/...` / `what-spinball-can-do.md` / `tell-spinball`; old stored notes/Spaces messages containing `@rowboat` or `tell-rowboat` are NOT migrated (breaking by design, stated here).
## Not verified
- No screenshot: `screencapture` has no display access on this machine (same blocker as `docs/fork/00-baseline.md`); window title, menu app name, and onboarding headline were read from source (`index.html:7`, `menu.ts:168,358,374`, `welcome-step.tsx:23`) and the booted renderer HTML returned 200 with that title, rather than pixels.
- No full chat turn: no provider credentials exist in this environment (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`OPENROUTER_API_KEY`/`OLLAMA_HOST`/`ROWBOAT_API_KEY` all absent, none created or written); the boot proof goes as far as the baseline Headless matrix (health, unauthorized, list, `onboarding:getStatus`, create, `sendMessage` → `No assistant model configured` pattern) plus a sandbox boot with the stub.
- Phone-home attribution beyond the stub: sandbox run showed stub `GET /v1/config` ×2 and 0× `/v1/me` (signed-out never asks billing); zero connections to `api.x.rowboatlabs.com`, PostHog, or `update.electronjs.org` were observed in the stub log, but no packet capture (`tcpdump`/`lsof` per host) was run to attribute the one transient Google TLS connection the integration verdict left open.
- `POSTHOG_KEY`/`VITE_PUBLIC_POSTHOG_KEY` were never set (verified absent in build env); `ROWBOAT_TELEMETRY=off` was exported for the sandbox boot.
## Shared files
For each file another lane also edits, the hunk verbatim in a diff block.
- `apps/x/packages/shared/src/brand.ts` (lane B created the six-value seam; this lane extends to twelve + renames product/executable/scheme):
```diff
 export const brand = Object.freeze({
-  productName: "Spinrun Desktop",
-  executableName: "spinrun-desktop",
+  productName: "Spinrun",
+  executableName: "spinrun",
   appBundleId: "ai.spinrun.desktop",
   updateRepo: "corneliu10/rowboat",
-  deepLinkScheme: "rowboat",
+  deepLinkScheme: "spinrun",
   upstream: "rowboatlabs/rowboat",
+  assistantName: "Spinball",
+  mentionHandle: "spinball",
+  companyName: "Spinrun",
+  siteUrl: "https://spinrun.ai",
+  docsUrl: "https://spinrun.ai/docs",
+  supportUrl: "https://spinrun.ai/support",
 });
```
- `apps/x/apps/main/brand.cjs` (lane B created the pick-list reader; this lane adds the new keys):
```diff
 module.exports = {
   productName: pick('productName'),
   executableName: pick('executableName'),
   appBundleId: pick('appBundleId'),
   updateRepo: pick('updateRepo'),
   deepLinkScheme: pick('deepLinkScheme'),
   upstream: pick('upstream'),
+  assistantName: pick('assistantName'),
+  mentionHandle: pick('mentionHandle'),
+  companyName: pick('companyName'),
+  siteUrl: pick('siteUrl'),
+  docsUrl: pick('docsUrl'),
+  supportUrl: pick('supportUrl'),
 };
```
- `apps/x/apps/main/package.json` (lane B set `Spinrun Desktop`; this lane shortens to `Spinrun` per the rebrand values):
```diff
-    "productName": "Spinrun Desktop",
+    "productName": "Spinrun",
```
- `apps/x/apps/main/forge.config.cjs` (lane B wired names/ids/repos from `brand.cjs`; this lane makes `spinrun://` primary with `rowboat://` fallback + Spinrun permission/homepage copy):
```diff
         protocols: [
-            { name: brand.productName, schemes: [brand.deepLinkScheme] },
+            // Intentional dual value for one release: spinrun:// primary, rowboat:// fallback so old invite links still open.
+            { name: brand.productName, schemes: [brand.deepLinkScheme, "rowboat"] },
         ],
         extendInfo: {
-            NSAudioCaptureUsageDescription: 'Rowboat needs access to system audio to transcribe meetings from other apps (Zoom, Meet, etc.)',
-            NSCameraUsageDescription: 'Rowboat uses your camera in video chat mode so the assistant can see you and give feedback (e.g. pitch practice).',
+            NSAudioCaptureUsageDescription: 'Spinrun needs access to system audio to transcribe meetings from other apps (Zoom, Meet, etc.)',
+            NSCameraUsageDescription: 'Spinrun uses your camera in video chat mode so the assistant can see you and give feedback (e.g. pitch practice).',
```
- `apps/x/packages/shared/src/index.ts` (lane B added `export * from './brand.js'`; this lane adds the mention helper export):
```diff
 export * from './brand.js';
+export * from './mention.js';
 export { PrefixLogger };
```
## Follow-up: logos (post-report, same lane)
- `apps/x/apps/renderer/public/logo-only.png` was byte-identical to upstream `apps/x/apps/main/icons/upstream/icon.png` (md5 `a652f8752279`, Rowboat) while `apps/x/apps/main/icons/icon.png` was already the Spinrun placeholder (md5 `146639293b87`, rasterized from `apps/x/apps/main/icons/spinrun-s.svg`). Replaced `logo-only.png` with the Spinrun raster (now md5 `146639293b87`, 800×800, used by `about-dialog.tsx:183` and `welcome-step.tsx:28` with unchanged `src`, so `about-dialog.test.tsx:61` still passes 3/3).
- Chrome extension icons were the same Rowboat bytes (`extension/icon.png` md5 `a652f8752279`); replaced `icon.png` (800) + `icons/icon16/32/48/128.png` with `rsvg-convert` rasters of `spinrun-s.svg` at matching sizes (manifest references unchanged).
- Mobile `assets/images/icon.png` + `splash-icon.png` (1024) + `android-icon-foreground.png` (512) replaced with Spinrun rasters at matching sizes (background/monochrome solids have no logo and were left; `app.json` name/scheme were already Spinrun).
- `apps/x/apps/main/icons/install-loading.gif` regenerated via `gen-install-loading.sh` with `FONT=/System/Library/Fonts/Geneva.ttf` (ImageMagickallation via brew; script text was already `Installing Spinrun` from Step 5, icon is now Spinrun): 110 frames, 480×320, 7397 bytes.
- `npm run rebrand:check` still exits 0 (binaries carry no `rowboat` word hits); `about-dialog.test.tsx` 3/3 pass.
## Follow-up F2
Branch `spike/f-rebrand`, six one-commit items on top of `8d91b16f`. `git status` was clean, `git pull --ff-only origin spike/f-rebrand` already up to date. Guard rails as lane F (repo-local, no secrets — `git diff --cached | grep -nE 'sk-ant-|spr_|phc_|AKIA'` before every commit, push to origin only, LICENSE/NOTICE untouched — `git diff origin/spike/integration -- LICENSE NOTICE` empty (0 lines), minimal diffs, no PR).
### Commits
- `111ab2f3` F2 item 1: google-setup.md Spinrun prose + gate checks root md (README, google-setup)
- `d3812a67` F2 item 2: code-session branch prefix spinrun/ via CODE_SESSION_BRANCH_PREFIX in shared
- `f1cbba52` F2 item 3: meetings folder spinrun/ via MEETINGS_FOLDER, dual-read prompt, hook test
- `1f77eb39` F2 item 4: API_URL api.spinrun.ai, APEX_URL spaces.spinrun.ai, stub README, dead-backend check
- `163399f8` F2 item 5: Spinrun describe copy, CFBundleDisplayName, spinrun-toolkit, install-arch comment
- `91811bf4` F2 item 6: tighten gate (line-start //, specific ids, rowboatlabs.com hard failure) + prose fixes
### 1. google-setup.md
- `google-setup.md:1` `# Connecting Google to Spinrun`; `:3` `Spinrun requires Google OAuth credentials…`; `:24` `` `Spinrun Integration` ``; `:29-31` first image plus new line `Screenshots below show the previous UI.` (screenshots kept, same `assets/google-setup/*.png` paths); `:67` `` `Spinrun` ``; `:97` `connect with Spinrun`; `:119` `` `Spinrun` ``; `:140` `into Spinrun`; `:142` `![Enter credentials in Spinrun](…)`. `grep -niE '\browboat\b' google-setup.md` clean.
- `tools/rebrand/check.mjs` `fileSkipped`: added `proposal.md` skip, replaced `if (file.endsWith('.md') && file !== 'README.md')` with checked `README.md`/`google-setup.md`, skipped `apps/x/`/`apps/harbor/`/`docs/` md, checked all other md (e.g. `tools/control-plane-stub/README.md` passes via identifiers). `node tools/rebrand/check.mjs` exits 0.
### 2. Branch prefix
- `apps/x/packages/shared/src/brand.ts:21` `export const CODE_SESSION_BRANCH_PREFIX = \`${brand.executableName}/\`;` (resolves `spinrun/`).
- `apps/x/packages/core/src/background-tasks/code-sessions.ts:4` imports it; `:294` `session.worktree?.branch ?? CODE_SESSION_BRANCH_PREFIX + session.id` (`??` fallback stays, stored branches kept).
- `apps/x/packages/core/src/code-mode/sessions/service.ts:6` imports it; `:224` `` `${CODE_SESSION_BRANCH_PREFIX}${sessionId}` ``.
- Tests: `apps/x/packages/shared/src/brand.test.ts` asserts `CODE_SESSION_BRANCH_PREFIX === 'spinrun/'` and `=== brand.executableName + '/'` (shared 326/326); `apps/x/packages/core/src/code-mode/sessions/workspaces.test.ts` new `creates new worktree branches under the spinrun/ prefix` expects `spinrun/s2` in `created.worktree.branch` and `worktreeAdd` arg (13/13 with file). Legacy `rowboat/s1` fixtures stay (stored branches, gate-allowlisted `"rowboat/…"`).
### 3. Meetings folder
- `apps/x/packages/shared/src/brand.ts:25` `export const MEETINGS_FOLDER = brand.executableName;` (`spinrun`).
- `apps/x/apps/renderer/src/hooks/useMeetingTranscription.ts:7` imports it; `:525` `` `knowledge/Meetings/${MEETINGS_FOLDER}/…` ``; `:532` suffixed variant same. Old notes stay on disk (no migration).
- `apps/x/packages/core/src/knowledge/sources/repo.ts:4` imports it; `:47-49` comment plus `artifactDir: path.join('knowledge', 'Meetings', MEETINGS_FOLDER)` (id `rowboat-meetings` stays for compat, quoted allowlist).
- `apps/x/packages/core/src/knowledge/inline_task_agent.ts:186` example now `` `knowledge/Meetings/spinrun/2026-03-30/…` `` plus `also read the legacy \`knowledge/Meetings/rowboat/<YYYY-MM-DD>/\``.
- Tests: shared `meetings folder follows the executable name` (5/5 in brand.test.ts); new `apps/x/apps/renderer/src/hooks/useMeetingTranscription.test.ts` (2/2) asserts `MEETINGS_FOLDER === 'spinrun'` and template resolves `knowledge/Meetings/spinrun/2026-03-30/meeting-….md`.
### 4. Defaults + dead-backend check
- `apps/x/packages/core/src/config/env.ts:1-2` `process.env.API_URL || 'https://api.spinrun.ai'` (dead today, intended).
- `apps/x/apps/mobile/src/lib/spaces/account.tsx:13` `?? 'https://spaces.spinrun.ai'` (dead today, intended).
- `tools/control-plane-stub/README.md:1,3-4` title `defaults updated lane F2`, stand-in for `https://api.spinrun.ai`.
- Dead-backend re-run (stub off, 2 min): `cd apps/x && env -u API_URL ROWBOAT_TELEMETRY=off npm run dev:sandbox -- --no-deps --workdir $PWD/.sandbox/f2-dead --name f2-dead` (ports server=53803 apps=53804 vite=53805), `/tmp/f2-dead.log` 337 lines. Only backend error is the sign-in path: `[OAuth] Starting connection flow for rowboat…` → `OAuth connection failed: TypeError: fetch failed` (`oauth-flows.js:308` ← `oauth:connect`), `[cause]: Error: getaddrinfo ENOTFOUND api.spinrun.ai` (`hostname: 'api.spinrun.ai'`), `grep -c "fetch failed"` = 1. `grep rowboatlabs` = zero hits (app does not phone Rowboat by default). Remainder is local first-run noise (ENOENT `recent-work-dirs.json`/`exa-search.json`, Google/Microsoft credential watchers `Sleeping…`, GraphBuilder/NoteTagging/InlineTasks idle). Quit cleanly (`[sandbox] app exited (code 0)`), no leftover `f2-dead` processes; workdir under git-ignored `.sandbox/`.
### 5. Minor
- `apps/x/packages/shared/src/rowboat-app.ts:35` `RESERVED. Spinrun MUST NOT execute this in V1.`; `:37` `Minimum compatible Spinrun version…` (field name `minRowboatVersion` stays, camelCase gate-invisible).
- `apps/x/apps/main/forge.config.cjs:236` `CFBundleDisplayName: brand.productName,` (see Not verified for plutil deviation).
- `apps/x/packages/core/src/composio/flows.ts:94` `` name: `spinrun-${toolkitSlug}` `` (`user_id: 'rowboat-user'` stays, quoted allowlist).
- `apps/x/scripts/install-arch.sh:2` `# Build the Spinrun desktop app…` (`ROWBOAT_SPACES`, `rowboat.conf` path stay, identifier allowlists).
### 6. Tighten the gate
- `tools/rebrand/check.mjs`: `/\/\/.*rowboat/i` → `/^\s*\/\/.*rowboat/i` (line-start only; `^\s*(//|#|*|<!--)` already covers full-line comments); dropped broad `/rowboat (provider|mode|…)/i`, replaced with specific `provider: "rowboat"`, `flavor: "rowboat"`, `which: "rowboat"`, `onboardingPath`, `config['rowboat']` (quoted/`rowboat:` shapes were already covered; prose in `//`/`*` comments stays covered by the line pattern).
- Added hard failure `ROWBOATLABS_URL = /https?:\/\/[^\s'"]*rowboatlabs\.com/i` with no allowlist except `fileSkipped` (NOTICE, `docs/fork/`, `proposal.md`, `apps/x/*.md`, fixtures) and README attribution line. `tools/control-plane-stub/README.md` old-URL parenthetical removed in same commit so the checked file carries zero such URLs.
- Required prose fixes (9 hits, else gate-red): `apps/x/packages/core/src/auth/tokens.ts:26` `Refreshing Spinrun access token`; `catalog.test.ts:279,289,302` `Spinrun provider` descriptions (quoted `'rowboat'` ids stay); `initial-selection.test.ts:108` `(Spinrun auto-select)`, `:111,118` `Spinrun initial pick`, `:124` `Spinrun task overrides` (`{ rowboat: … }`, `'rowboat'` args stay); `repo.test.ts:108` `Spinrun provider` (`'rowboat'` args stay).
- `npm run rebrand:check` in `apps/x` exits 0 (`rebrand:check OK — no uncovered whole-word "rowboat" hits`, zero URL failures).
### Tests
- Typecheck 6/6 exit 0: `typecheck:shared`, `typecheck:core`, `typecheck:server`, `typecheck:client`, `typecheck:renderer` (apps/x), Harbor `pnpm typecheck` (protocol + server).
- `test:shared` 21 files / 326 pass (was 324; +branch +meetings); `test:server` (apps/x) 4 / 27 pass; `test:renderer` 120 / 1021 pass (was 119/1019; +transcription test file); `apps/main` vitest 3 / 15 pass; Harbor server `vitest run --maxWorkers=2` 25 / 381 pass, 1 skipped.
- `test:core` load-flaky as baseline (`docs/fork/00-baseline.md` §Red tests): full runs 6–7 failed / ~1000 passed (persistent `chatgpt-auth.test.ts` ×3, `catalog.test.ts` (runtime/tools) ×1, `status-tracker.test.ts` ×1; flakes `google-client-factory`, `git/service`, `orgs-oauth`/`orgs-session` timeouts). Touched files pass in isolation: `workspaces` + `initial-selection` + `repo` 3 files / 34 pass; `models/catalog.test.ts` 16/16.
- `npm run rebrand:check` exits 0. `git diff origin/spike/integration -- LICENSE NOTICE` empty.
### Package
- `cd apps/x/apps/main && SKIP_CODE_SIGNING=1 ROWBOAT_SKIP_CODE_SIGNING=1 NODE_OPTIONS=--max-old-space-size=6144 npm run package` exit 0 (`out/Spinrun-darwin-arm64/Spinrun.app`, 423M). `plutil -p …/Info.plist`: `CFBundleName => Spinrun`, `CFBundleIdentifier => ai.spinrun.desktop`, executable `Contents/MacOS/spinrun`. See Not verified for DisplayName.
- Window title / menu / onboarding re-confirmed from source (unchanged by F2): `apps/x/apps/renderer/index.html:7` `<title>Spinrun</title>`; `apps/x/apps/main/src/menu.ts:168` `About Spinrun`, `:358` `Spinrun on GitHub`, `:374` `About Spinrun`; `welcome-step.tsx:23` `Welcome to Spinrun`.
### Decisions
- `CODE_SESSION_BRANCH_PREFIX` and `MEETINGS_FOLDER` live in `shared/src/brand.ts` (built from `brand.executableName`), exported via existing `export * from './brand.js'`; core/renderer import from `@x/shared/dist/brand.js` (rebuilt `shared` dist before tests).
- `rowboat-meetings` source id stays (on-disk config compat); only `artifactDir` moves to spinrun. Stored `rowboat/*` branches and `Meetings/rowboat/` notes stay (dual-read prompt).
- Mobile `ACCOUNT_KEY`/`ORGS_CACHE_KEY` (`rowboat.spaces…`) stay (storage keys, gate-allowlisted); only `APEX_URL` default moves.
- Composio `user_id: 'rowboat-user'` stays (connection identity, allowlisted); only the managed-auth display `name` becomes `spinrun-<toolkit>`.
### Not verified
- `CFBundleDisplayName`: `forge.config.cjs:236` sets `CFBundleDisplayName: brand.productName` (`node -e` resolves `Spinrun`), package exit 0, but `plutil`/`Info.plist` shows `<string>spinrun</string>` (lowercase, same as lane-F baseline without the key). Packager appears to derive DisplayName from `executableName`, overriding `extendInfo`. Implemented as prompted; actual value recorded here, not faked to `Spinrun`.
- No screenshot (same blocker as lane F: headless session, `screencapture` no display); title/menu/headline read from source.
- No full chat turn (no provider keys); dead-backend run above is the extent of live proof.
- Pushed below; no PR (per guard rails).
